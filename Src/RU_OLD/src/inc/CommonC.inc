//********************************************************************************
//                                                        (c) корпорация Галактика
// Галактика 7.12 - константы
// Общие константы (только для сборки vip-файлов, не для паскаля)
//********************************************************************************

  ZeroDate     = date(0, 0, 0);
  ZeroTime     = time(0, 0, 0, 0);
  ZeroDateTime = _DateTime(0, 0, 0, 0, 0, 0, 0);

//********************************************************************************

//------------------------------------------------------------------------------
// Констаны для функций Атлантиса
//   SetWindowHelpCtx
//   SetHelpAndStatusContext
//   MessageWithHelp
// До решения 102.106809 контанты будут прикладным
// Затем появятся в Атлантисе и будут строковыми:
//   hcUseOwner    = 'NoContext'
//   hcKeepCurrent = ''
//------------------------------------------------------------------------------
#if(#__TOOLVERSION__ < 5005.000)
  hcUseOwner    =  0; // Использовать контекст контейнера (эквивалентно hcNoContext)
  hcKeepCurrent : integer = -1; // Не изменять текущий контекст
#end
//------------------------------------------------------------------------------
