Var
   CurDSE   : Comp;         //текущая матценность
   pCurType : Comp;         //текущий тип МЦ
   pCurGroup: Comp;         //текущая группа
   CurTech  : Comp;         //текущий техпроцесс
   CurVariant  : Comp;      //текущий вариант плана выпуска

   Filter: Byte;            //значение фильтра выбора матценностей
                            //сумма некоторой комбинации констант
                            //например, Filter := BuyDSE+Manuf+Ready+Material

   pCurName: String;        //текущее наименование матценности
   CurBegName: String;      //начальные буквы наименования матценностей
                            //служит для выбора блока матценностей по нач.буквам

   CurNova: Byte;           //в версии 5.50 не используется
   pCurKodGroup: String;    //код текущей группы матценностей
   LengthGroup: Byte;       //длина кода группы матценностей
   pCurPodr: Comp;          //текущее подразделение
   tDate: Word;             //служит для запоминания даты

   pCurYear: Word;          //год по умолчанию    (зависят от настройки )
   pCurMonth: byte;         //месяц по умолчанию  (на год и месяц       )
   pCurDate: Date;          //дата по умолчанию
   pCurKvart: Byte;         //квартал по умолчанию
   pCurDay: Byte;           //день месяца по умолчанию
   pCurDoW: Byte;           //день недели по умолчанию
   FirstDate : Date;        //дата на 1-е число месяца по умолчанию
                            //или дата, с которой смотреть изменение п-у цен

   TodayYear: word;         //текущий год   (зависят только от        )
   TodayMonth: Byte;        //текущий месяц (системной даты компьютера)


                       //Переменные для передачи параметров
   CurCeh :   Comp;         //текущий цех
   MainCeh:   Longint;      //=1 код производственной функции для цехов основного производства
   HelpCeh:   Longint;      //=2 код производственной функции для цехов вспомогательного производства
   CurEdNorm: Double;       //единица нормирования по умолчанию (объем для нормирования)
   VolPart:   Double;       //объем партии по умолчанию
   pCurZakaz: Comp;         //текущий производственный заказ
   pCurSmeta: Word;         //текущая смета
   CurPF:     Byte;         //текущее значение переключателя план/факт
   CurItem:   Byte;         //вариант выбора диапазона матценностей (тип,группа,блок)
   PickDate:  Byte;         //выбранная дата
   FlagSale:  Boolean;      //признак продаваемости
   FlagOut:   Byte;         //вариант выдачи отчетов
   FlagVol:   Byte;         //вариант выбора объема
   Flag1:     Byte;         //флаги-признаки
   Flag2:     Byte;         //
   FlagCasePdrz: Byte;      //признак: 0-по предприятию; 1-по цехам
   MaxSimbol:  Byte;      //максимальное количество символов каретки для отчетов
   Detail:     Byte;        //
   Detail2:    Byte;        //
   Stratagy:   Byte;        //стратегия расчета планово-учетных цен
   Julian:     Date;        //выбранная текущая дата
   BegJulian:  Date;        //дата, с которой смотреть изменения
   MaxLevel:   word;        //глубина разузлования (макс.число уровней вхождения)
   DebugFlag: Byte;         //признак режима отладки (для выдачи сообщений)
   Scale: String;           //наименование нац.денежной единицы
   SumIsPlan: Boolean;      //показывает в каком модуле находимся: ТЭП или РФЗ
   MaxDepNum: Byte;         //
   ProtIsOutp: Boolean;     //признак выдачи протокола при расчете себестоимости
   UserOutpStrLenSup: Byte; //
   TPPMode: Byte;           //признак, что находимся в модуле ТПП
   CurEdIzm: Comp;          //единица измерения по умолчанию
